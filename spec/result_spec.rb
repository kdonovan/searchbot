require 'spec_helper'

describe Searchbot::Results::Base do

  it "filters properly" do
    pending "Implement specs to test result filtering, both in search URL and filtering out results"
    expect(false).to be true
  end

end
